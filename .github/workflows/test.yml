name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  initjob:
    runs-on: ubuntu-latest
    env:
      owner_name: quangtd
    steps:
      - run: ls -lat 
      - run: pwd
      - run: echo $TEST_SECRET $owner_name
